# algosystem

Published Python library placeholder for the future `algosystem` migration.

This directory is intentionally minimal until the source repository is imported.
Its manifest and tests exist so the monorepo CI shape can be validated before
the code move.

[project]
name = "algosystem"
version = "0.0.0"
description = "AlgoGators quantitative research primitives"
requires-python = ">=3.9"
readme = "README.md"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/algosystem"]

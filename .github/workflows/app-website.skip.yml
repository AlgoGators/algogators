name: app-website · quality (skip)

on:
  pull_request:
    paths-ignore:
      - 'apps/website/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/_quality-node.yml'
      - '.github/workflows/app-website.quality.yml'

permissions: {}

jobs:
  gate:
    name: app-website quality
    runs-on: ubuntu-latest
    steps:
      - run: echo "apps/website untouched; skipping member CI"

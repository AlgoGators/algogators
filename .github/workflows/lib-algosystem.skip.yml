name: algosystem · quality (skip)

on:
  pull_request:
    paths-ignore:
      - 'libs/algosystem/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/lib-algosystem.quality.yml'

permissions: {}

jobs:
  gate:
    name: lib-algosystem quality
    runs-on: ubuntu-latest
    steps:
      - run: echo "libs/algosystem untouched; skipping member CI"

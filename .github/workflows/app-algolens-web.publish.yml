name: algolens web · publish

on:
  push:
    tags: ['algolens-web/v*']
  workflow_dispatch:
    inputs:
      dry-run:
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/_quality-node.yml
    with:
      path: apps/algolens-web
      coverage-min: 50
      bundle-budget-kb: 500
    secrets: inherit

  publish:
    needs: quality
    uses: ./.github/workflows/_publish-static-site.yml
    with:
      path: apps/algolens-web
      target: pages
      dry-run: ${{ inputs.dry-run || false }}
    permissions:
      contents: read
      pages: write
      id-token: write

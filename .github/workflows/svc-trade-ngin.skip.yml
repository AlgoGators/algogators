name: trade-ngin · quality (skip)

on:
  pull_request:
    paths-ignore:
      - 'services/trade-ngin/**'
      - 'vcpkg.json'
      - 'CMakePresets.json'
      - '.github/workflows/_quality-cpp.yml'
      - '.github/workflows/svc-trade-ngin.quality.yml'
      - 'platform/contracts/**'

permissions: {}

jobs:
  gate:
    name: svc-trade-ngin quality
    runs-on: ubuntu-latest
    steps:
      - run: echo "services/trade-ngin untouched; skipping member CI"

name: research-api · quality (skip)

on:
  pull_request:
    paths-ignore:
      - 'services/research-api/**'
      - 'libs/research-core/**'
      - 'libs/platform-db/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/svc-research-api.quality.yml'

permissions: {}

jobs:
  gate:
    name: svc-research-api quality
    runs-on: ubuntu-latest
    steps:
      - run: echo "services/research-api untouched; skipping member CI"

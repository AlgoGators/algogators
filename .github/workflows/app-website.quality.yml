name: app-website · quality

on:
  pull_request:
    paths:
      - 'apps/website/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/_quality-node.yml'
      - '.github/workflows/app-website.quality.yml'
  push:
    branches: [main]
    paths:
      - 'apps/website/**'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - '.github/workflows/_quality-node.yml'
      - '.github/workflows/app-website.quality.yml'
  workflow_dispatch:

concurrency:
  group: app-website-quality-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/_quality-node.yml
    with:
      path: apps/website
      node-versions: '["20"]'
      runners: '["ubuntu-latest"]'
      coverage-min: 50
      bundle-budget-kb: 500
    secrets: inherit

  gate:
    name: app-website quality
    if: always()
    needs: [quality]
    runs-on: ubuntu-latest
    steps:
      - name: Report result
        run: |
          if [ "${{ needs.quality.result }}" != "success" ]; then
            echo "::error::quality result was ${{ needs.quality.result }}"
            exit 1
          fi

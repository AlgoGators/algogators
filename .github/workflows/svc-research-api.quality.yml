name: research-api · quality

on:
  pull_request:
    paths:
      - 'services/research-api/**'
      - 'libs/research-core/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/svc-research-api.quality.yml'
  push:
    branches: [main]
    paths:
      - 'services/research-api/**'
      - 'libs/research-core/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/svc-research-api.quality.yml'
  workflow_dispatch:

concurrency:
  group: svc-research-api-quality-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/_quality-python.yml
    with:
      path: services/research-api
      python-versions: '["3.11"]'
      runners: '["ubuntu-latest"]'
      coverage-min: 55
      strict-types: false
      build-check: false
    secrets: inherit

  gate:
    name: svc-research-api quality
    if: always()
    needs: [quality]
    runs-on: ubuntu-latest
    steps:
      - name: Report result
        run: |
          if [ "${{ needs.quality.result }}" != "success" ]; then
            echo "::error::quality result was ${{ needs.quality.result }}"
            exit 1
          fi

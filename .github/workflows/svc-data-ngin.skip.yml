name: data-ngin · quality (skip)

on:
  pull_request:
    paths-ignore:
      - 'services/data-ngin/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/svc-data-ngin.quality.yml'
      - 'platform/contracts/**'

permissions: {}

jobs:
  gate:
    name: svc-data-ngin quality
    runs-on: ubuntu-latest
    steps:
      - run: echo "services/data-ngin untouched; skipping member CI"

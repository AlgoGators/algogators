name: algosystem · quality

on:
  pull_request:
    paths:
      - 'libs/algosystem/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/lib-algosystem.quality.yml'
  push:
    branches: [main]
    paths:
      - 'libs/algosystem/**'
      - 'uv.lock'
      - 'pyproject.toml'
      - '.github/workflows/_quality-python.yml'
      - '.github/workflows/lib-algosystem.quality.yml'
  workflow_dispatch:

concurrency:
  group: lib-algosystem-quality-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/_quality-python.yml
    with:
      path: libs/algosystem
      python-versions: '["3.9","3.11","3.13"]'
      runners: '["ubuntu-latest","macos-latest","windows-latest"]'
      coverage-min: 85
      strict-types: true
      build-check: true
    secrets: inherit

  gate:
    name: lib-algosystem quality
    if: always()
    needs: [quality]
    runs-on: ubuntu-latest
    steps:
      - name: Report result
        run: |
          if [ "${{ needs.quality.result }}" != "success" ]; then
            echo "::error::quality result was ${{ needs.quality.result }}"
            exit 1
          fi

# contracts

Shared schema boundary for data exchanged between services.

The real schemas have not been promoted yet. When they are, hand-edited schema
files live under `schemas/` and generated language bindings live under
`codegen/generated/`.

# workflow templates

`platform/ci/new_service.py` owns the concrete workflow templates. This
directory exists as the stable place for future file-backed templates if the
inline templates grow too large.
